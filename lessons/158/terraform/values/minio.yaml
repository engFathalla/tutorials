---
nodeSelector:
  service: s3
tolerations:
  - key: s3
    operator: Exists
    effect: NoSchedule

persistence:
  enabled: true
  storageClass: ssd
  size: 97Gi

service:
  type: LoadBalancer

auth:
  rootUser: admin
  rootPassword: devops123
