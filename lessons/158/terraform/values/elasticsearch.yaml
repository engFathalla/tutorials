---
master:
  masterOnly: false
  nodeSelector:
    service: elasticsearch
  tolerations:
    - key: elk
      operator: Exists
      effect: NoSchedule
  persistence:
    enabled: false

data:
  replicaCount: 0

coordinating:
  replicaCount: 0

ingest:
  replicaCount: 0
